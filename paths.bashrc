if [[ -z "$isDefined" ]]; then
	readonly PATH_dev="C:/dev"
	readonly PATH_books="C:/dev/books/reading_now"
	readonly PATH_projects="C:/dev/projects"
	readonly PATH_desktop="~/onedrive/desktop"
fi